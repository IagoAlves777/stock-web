export interface Product {
  id: string;
  name: string;
  price: string;
  amount: number;
}

export interface SupermarketCarProduct {
  id: string;
  price: number;
  amount: number;
  name: string;
}
